[server]
headless = true
maxUploadSize = 200

[browser]
gatherUsageStats = false

# Refined warm editorial theme — matches src/theme.py and web/lib/theme.js.
[theme]
base = "light"
primaryColor = "#1f6f8b"
backgroundColor = "#f4efe7"
secondaryBackgroundColor = "#ffffff"
textColor = "#1c1b1a"
font = "sans serif"
